apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base
  - ../../../components/operators/openshift-gitops/instance/overlays/lab

resources:
  - argocd-ssh-known-hosts-cm.yaml
  - repo-cluster-config-sealed.yaml
