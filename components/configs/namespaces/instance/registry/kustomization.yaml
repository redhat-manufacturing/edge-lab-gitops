apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: registry

resources:
  - namespace.yaml
  - puller-rolebinding.yaml
  - puller-sa.yaml
  - pusher-rolebinding.yaml
  - pusher-sa.yaml
